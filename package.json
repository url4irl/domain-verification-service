{
  "name": "domain-verification-service",
  "version": "0.1.0",
  "description": "Verify and instruct domain configuration, enabling you to proceed pointing to them after DNS TXT record verification",
  "scripts": {
    "start": "ts-node lib/api.ts",
    "dev": "concurrently -n backend,db,db-studio -c blue.bold,cyan.bold,green.bold \"wait-on tcp:5432 && ts-node-dev --respawn --transpile-only lib/api.ts\" \"docker compose -f ./dev/docker-compose.yml up\" \"wait-on tcp:5432 && drizzle-kit studio\"",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "concurrently -n db,migration -c blue.bold,cyan.bold  \"wait-on tcp:5432 && drizzle-kit migrate\" \"docker compose -f ./dev/docker-compose.yml up\"",
    "test": "pnpm run test:setup && jest && pnpm run test:teardown",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:setup": "docker compose -f ./dev/docker-compose.yml up -d && wait-on tcp:5432 && npm run db:setup-test",
    "test:teardown": "docker compose -f ./dev/docker-compose.yml down",
    "db:setup-test": "DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres_test drizzle-kit migrate"
  },
  "keywords": [],
  "author": "Marcelo Cardoso",
  "license": "ISC",
  "packageManager": "pnpm@10.6.5",
  "devDependencies": {
    "@types/body-parser": "^1.19.6",
    "@types/express": "^4.17.23",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.10",
    "@types/pg": "^8.15.4",
    "@types/supertest": "^6.0.3",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "concurrently": "^9.2.0",
    "drizzle-kit": "^0.31.4",
    "jest": "^30.0.4",
    "supertest": "^7.1.3",
    "ts-jest": "^29.4.0",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^17.2.0",
    "drizzle-orm": "^0.44.2",
    "express": "^4.21.2",
    "pg": "^8.16.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  }
}
